個人用の~/.vimです。

=初期設定
```
$ git clone https://github.com/longicorn/.vim.git ~/.vim
$ cp ~/.vim/.vimrc ~/
```

https://github.com/Shougo/dein.vim をインストール

vimを起動してpluginをインストール
```
:call dein#install()
```
